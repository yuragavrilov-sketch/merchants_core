spring:
  cloud:
    config:
      enabled: false
    vault:
      enabled: false
  datasource:
    url: jdbc:h2:mem:merchants_core;MODE=Oracle;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
